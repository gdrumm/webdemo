<div class="jumbotron">
<h3>Rent your items to <br>
users nearby <br>
or find something <br>
you need!</h3>
<h4>No hassle of <br>
contacting a store <br> 
and find a solution for <br>
any time of the day!</h4>
</div>
  <div class="center">
    <div class="row">
    <% @listings.each do |listing| %>
    <div class="col-md-3">
      <div class="thumbnail">
      <%= link_to image_tag(listing.image.url), listing %>
          <div class="caption">
            <h3><%= listing.name %></h3>
            <p><%= number_to_currency(listing.price) %></p>
              <p><%= "Renter: #{listing.user.name}" %></p>
          </div>
      </div>
  </div>
    <% end %>
  </div>
</div>

<br>

<% if user_signed_in? %>
  <%= link_to 'New Listing', new_listing_path, class: "btn btn-link" %>
<% end %>
